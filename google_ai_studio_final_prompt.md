# Google AI Studio 최종 제작 프롬프트

아래 요구사항을 만족하는 모바일 중심 풀스택 웹앱을 만들어줘. 제품 작업명은 **“우리의 첫 이야기”**이며, 임신부터 출산 후까지 엄마와 배우자가 함께 기록하고 꾸미는 비공개 감성 성장 앨범이다. 단순한 정적 목업이 아니라 실제 입력, 저장, 초대, 두 기기 실시간 공동편집, 사진 업로드, AI 제안, 필터가 동작해야 한다.

## 1. 제품 목표

- 사진·감정·일기를 5분 안에 기록한다.
- 원하는 기록을 날짜/임신 주수/아기 개월 수, 기록 종류, 감정으로 1분 안에 찾는다.
- 엄마와 배우자가 서로 다른 기기에서 같은 가족 앨범을 실시간으로 함께 편집한다.
- 임신 확인, 첫 초음파, 첫 태동, 출산, 첫 웃음처럼 특별한 시기마다 여러 기록을 AI가 아이에게 보내는 성장 이야기 한 장으로 엮는다.
- AI가 부모의 원문을 덮어쓰거나 기록에 없는 사실을 만들어내지 않게 한다.

## 2. 구현 기술과 프로젝트 구조

다음 구성을 기본으로 사용하되, 현재 환경과 호환되는 최신 안정 API를 사용해 전체 프로젝트를 생성해줘.

- 프런트엔드: React + TypeScript + Vite, 모바일 우선 반응형 웹
- 라우팅: React Router
- 상태: React hooks와 작은 전역 store. 불필요한 대형 상태 라이브러리는 사용하지 않는다.
- 인증: Firebase Authentication의 익명 인증
- 데이터: Cloud Firestore 실시간 리스너
- 사진: Cloud Storage for Firebase
- 서버 로직: Firebase Callable Cloud Functions
- AI: Cloud Function 내부에서 `@google/genai`로 Gemini API 호출. 모델명은 `GEMINI_MODEL` 환경변수로 설정하고 클라이언트에 API 키를 노출하지 않는다.
- 동시 편집: Yjs CRDT. 텍스트는 `Y.Text`, 사진 순서는 `Y.Array`, 스티커와 공통 메타데이터는 `Y.Array`/`Y.Map`으로 관리한다.
- 스타일: CSS variables와 컴포넌트 단위 CSS. 과도한 UI 프레임워크 없이 접근 가능한 기본 요소를 사용한다.

다음 파일과 설정을 포함해줘.

- `/src` 전체 앱 코드
- `/functions` 초대 검증, Gemini 호출, Yjs 스냅샷 압축용 서버 코드
- `firestore.rules`, `storage.rules`, `firestore.indexes.json`
- `firebase.json`, `.env.example`, `README.md`
- Firebase Emulator Suite로 실행 가능한 로컬 개발 설정
- 최소 단위 테스트와 주요 흐름 통합 테스트

## 3. 보안 원칙

- 전통적인 이메일 회원가입은 사용하지 않는다. 앱 시작 시 `signInAnonymously`로 UID를 만든다.
- 가족 앨범은 `workspace` 단위로 격리한다.
- Firestore와 Storage는 `workspaces/{workspaceId}/members/{uid}`가 존재하는 인증 사용자만 접근하게 한다.
- 초대 링크에는 추측하기 어려운 임의 token만 포함하고 6자리 PIN은 URL에 넣지 않는다.
- PIN은 평문으로 저장하지 않는다. Cloud Function에서 salt를 사용해 hash하고, 검증은 서버에서만 수행한다.
- `createInvite`는 소유자만 호출할 수 있고, token과 PIN은 생성 직후 한 번만 반환한다.
- `joinWorkspace`는 익명 인증 UID, token, PIN, 표시 이름을 받아 검증 후 member 문서를 만든다.
- PIN 반복 실패에는 서버 측 지연 또는 임시 잠금을 적용한다.
- 소유자는 초대 링크를 만료하거나 재발급할 수 있다.
- 일기 본문, 사진 URL, 초대 token, PIN을 분석 로그에 남기지 않는다.
- 사진 업로드 전 모바일 크기로 축소하고 EXIF 위치 정보를 제거한다.
- Gemini API 키와 Firebase Admin 권한은 절대 클라이언트 번들에 포함하지 않는다.

## 4. 역할과 권한

### 엄마/소유자

- 전체 타임라인 보기, 기록 작성/수정/삭제
- 사진, 감정, 스티커, 글 실시간 공동편집
- 변경 이력 보기와 복원
- 초대 링크 생성, 만료, 재발급
- 가족 앨범 전체 삭제

### 배우자/공동작업자

- 초대 링크와 6자리 PIN으로 참여
- 임신부터 육아까지 전체 타임라인과 모든 기록 보기
- 기록 작성/수정/삭제
- 사진, 감정, 스티커, 글 실시간 공동편집
- 변경 이력 보기와 복원
- 초대 링크 관리와 가족 앨범 전체 삭제는 불가

## 5. 시각 디자인

“따뜻한 손그림 스크랩북 + 여백이 넓은 파스텔 미니멀”을 결합한다.

- 배경: 아이보리 종이 질감. 텍스트 가독성을 해치지 않는 매우 약한 노이즈만 사용한다.
- 색상: 베이비 핑크, 크림, 세이지, 라벤더 중심의 저채도 파스텔
- 기본 토큰 예시:
  - `--paper: #FFF9F2`
  - `--ink: #403A3A`
  - `--muted: #716A6A`
  - `--rose: #C97B84`
  - `--pink-soft: #F9E7EA`
  - `--sage-soft: #E7F0E8`
  - `--lavender-soft: #EEEAF8`
  - `--line: #DED6D1`
- 손그림 스티커, 얇은 마스킹테이프, 작은 낙서 요소를 제한적으로 사용한다.
- 입력 화면은 장식을 줄이고 큰 버튼, 넓은 여백, 단순한 카드로 구성한다.
- 앨범 감상 화면은 사진과 스티커가 돋보이는 스크랩북 구성을 사용한다.
- 엄마와 배우자는 서로 다른 파스텔 색상과 이름으로 표시하되, 색상만으로 구분하지 않는다.
- 주요 터치 영역은 최소 44×44px이다.
- 모바일 기준 폭 390px에서 완성도 있게 보이고 태블릿/데스크톱에서도 가운데 정렬된 읽기 폭을 유지한다.

## 6. 전역 내비게이션

하단 내비게이션은 다음 네 영역으로 구성한다.

1. 타임라인
2. 가운데 강조된 “새 기록” 버튼
3. 이야기
4. 설정

모든 화면에서 자동 저장 상태를 `저장 중`, `저장됨`, `오프라인`으로 명확히 표시한다. 공동편집 중이면 상단에 접속 중인 참여자의 이름/색상/상태를 보여준다.

## 7. 최초 설정

처음 앱을 연 소유자에게 다음만 입력받는다.

- 아기의 태명 또는 이름
- 임신 중이면 출산 예정일, 출산 후이면 생년월일
- 엄마와 배우자의 표시 이름

출산 예정일로 임신 주수를 자동 계산하고, 출생일이 등록되면 아기 개월 수 계산으로 전환한다. 각 기록에서 자동 계산값을 수정할 수 있고 그 수정은 해당 기록에만 적용한다.

설정 완료 후 가족 앨범을 만들고 초대 링크와 6자리 PIN을 생성할 수 있게 한다.

## 8. 초대 참여 흐름

1. 소유자가 설정의 `배우자 초대`를 누른다.
2. 서버가 초대 token과 6자리 PIN을 생성한다.
3. 화면에 공유 링크, PIN, 링크 복사, PIN 복사, 공유하기 버튼을 표시한다.
4. 배우자가 링크를 열면 초대 참여 화면이 나타난다.
5. 배우자는 6자리 PIN과 표시 이름을 입력한다.
6. 인증에 성공하면 전체 타임라인에 참여하고 이후 세션에서는 자동 복구한다.
7. 링크가 만료되었거나 PIN이 틀리면 입력란 아래에 구체적인 오류를 보여준다.

## 9. 타임라인

- 임신부터 출산 후까지 모든 기록을 기본 최신순으로 보여준다. 오래된 순 전환도 제공한다.
- 카드에는 대표 사진 또는 감성 배경, 제목, 날짜, 임신 주수/아기 개월 수, 기록 종류, 감정, 작성자를 표시한다.
- 기록을 누르면 감성 앨범 한 페이지로 연다.
- 특별 시기 카드에는 성장 이야기 상태 배지를 표시한다: `후보`, `검토 중`, `완성`.
- 빈 상태에는 “첫 기록을 남겨보세요”와 새 기록 버튼을 보여준다.

### 필터

하단 시트에서 다음 조건을 여러 개 조합할 수 있게 한다.

- 날짜 범위, 임신 주수, 아기 개월 수
- 일상, 검진, 태동, 편지
- 행복, 설렘, 감동, 걱정, 피곤

적용 중인 조건을 상단 칩으로 표시하고 개별 해제와 전체 초기화를 제공한다. 자유 텍스트 검색은 이번 버전에서 구현하지 않는다.

## 10. 새 기록 흐름

`새 기록`을 누르면 먼저 종류를 선택한다.

- 일상
- 검진
- 태동
- 편지

공통 요소는 작성자, 날짜, 주수/개월 수, 글, 감정, 사진, 스티커다. 종류별 질문은 다음과 같다.

### 일상

- 오늘 가장 기억하고 싶은 순간
- 오늘의 이야기

### 검진

- 병원
- 검진일
- 임신 주수
- 초음파/검진 결과
- 다음 예약
- 느낀 점

### 태동

- 언제
- 어디서
- 어떤 느낌
- 당시 상황
- 배우자의 반응

### 편지

- 아이에게 꼭 전하고 싶은 말

작성 화면 상단에는 엄마·배우자 탭을 두고 언제든 전환할 수 있게 한다. 현재 선택 작성자의 이름과 색상 라벨을 표시한다.

## 11. 필수값과 감정

- 글과 감정은 필수다.
- 사진과 스티커는 선택이다.
- 글이 비어 있을 때 저장하면 입력란 아래에 `오늘의 이야기를 적어주세요`를 표시한다.
- 감정이 비어 있을 때 `오늘의 감정을 선택해주세요`를 표시한다.
- 필수값이 없으면 저장하지 않는다. 값을 입력하면 오류는 즉시 사라진다.
- 감정은 행복, 설렘, 감동, 걱정, 피곤 중 정확히 하나만 선택한다.

## 12. 사진

- 한 기록에 최대 5장까지 업로드한다.
- 사진마다 설명을 따로 받지 않는다. 사진 묶음 전체를 하나의 일기로 설명한다.
- 업로드 진행률, 실패 재시도, 삭제를 제공한다.
- 작성 화면에서 드래그로 순서를 변경한다.
- 앨범에서는 한 장씩 크게 보여주고 좌우 스와이프로 넘긴다.
- 현재 순서와 전체 장수를 `2 / 5` 형태로 표시하고 페이지 점과 이전/다음 접근 수단을 함께 제공한다.

## 13. 스티커

- 현재 기록 종류와 감정에 맞는 추천 스티커를 먼저 보여준다.
- `전체 보기`에서 하트, 별, 아기용품, 계절 카테고리를 탐색한다.
- 한 기록에서 스티커 개수 제한은 없다.
- 터치로 이동하고 핀치로 크기와 각도를 조절한다.
- 길게 눌러 복제 또는 삭제한다.
- 스티커 인스턴스는 `stickerId, x, y, scale, rotation, zIndex, createdBy`를 가진다.
- 공동편집 중 상대가 선택한 스티커에는 상대 이름과 색상 테두리를 표시한다.

## 14. 기록별 AI 제안

글과 감정이 모두 입력되면 사용자가 버튼을 누르지 않아도 자동으로 제안을 생성한다. 단, 매 키 입력마다 호출하지 말고 800~1200ms debounce를 적용하고 이전 요청을 취소한다.

AI 제안 항목:

- 일기 제목 1개
- 짧은 요약 2~3문장
- 선택한 감정에 어울리는 아이에게 건네는 문장 1개
- 앱에 실제 존재하는 스티커 ID 3~6개

제안은 작은 미리보기 영역에 표시하고 자동 적용하지 않는다. 부모가 원하는 항목만 선택하고 문장을 수정한 뒤 `적용`을 눌러야 기록에 반영한다. 선택하지 않은 제안은 저장하지 않는다. 부모의 원문은 별도 필드에 항상 유지한다.

AI 오류 시 `다시 시도`와 `AI 제안 없이 계속하기`를 함께 보여주고, 원문 기록 저장을 막지 않는다.

AI 응답은 JSON schema로 강제한다.

```json
{
  "title": "string",
  "summary": "string",
  "emotionalLine": "string",
  "stickerIds": ["string"]
}
```

AI system instruction에는 다음을 반드시 포함한다.

- 부모가 입력한 사실만 사용한다.
- 의료 판단이나 진단을 하지 않는다.
- 부모가 말하지 않은 감정, 사건, 날짜, 행동을 만들지 않는다.
- 따뜻하지만 과장되지 않은 한국어로 쓴다.
- 부모의 문체와 표현을 최대한 유지한다.
- 존재하는 스티커 ID 목록 밖의 값을 만들지 않는다.

## 15. 감성 앨범 페이지

- 타임라인 카드를 누르면 사진, 일기, 감정, 스티커, 날짜/주수·개월 수, 작성자가 한 페이지에 열린다.
- 사진이 있으면 상단 사진 캐러셀, 없으면 종이 질감 감성 배경을 사용한다.
- 스티커는 작성 화면에서 저장한 위치·크기·각도를 재현한다.
- 엄마와 배우자의 원문을 작성자 라벨과 함께 구분해 표시한다.
- 편집 버튼을 누르면 같은 화면이 실시간 공동편집 모드로 바뀐다.

## 16. 특별 시기 AI 성장 이야기

다섯 개 특별 시기를 기본 제공한다.

1. 임신을 처음 확인한 날
2. 첫 초음파를 본 날
3. 첫 태동을 느낀 날
4. 아기가 태어난 날
5. 처음 웃어준 날

관련 기록이 생기면 `이야기` 탭에 성장 이야기 후보를 만든다. 부모가 후보를 열면 AI가 관련 기록의 원문, 감정, 날짜/주수·개월 수, 부모가 선택한 사진만 사용해 2~4개 짧은 문단의 초안을 만든다.

- 초안은 자동 저장하지 않는다.
- 부모가 문장 톤, 내용, 사진을 수정하고 확인한 뒤 저장한다.
- 완성 페이지에는 엄마·배우자의 원문과 AI 성장 이야기를 함께 보관한다.
- 기록 밖 사실을 추가하면 안 된다.
- `초안 생성 중`, `수정 필요`, `확인 완료` 상태를 명확히 보여준다.

AI 응답은 다음 JSON schema를 사용한다.

```json
{
  "milestoneType": "pregnancy_confirmed | first_ultrasound | first_movement | birth | first_smile",
  "title": "string",
  "story": "string",
  "sourceRecordIds": ["string"],
  "suggestedPhotoIds": ["string"]
}
```

## 17. 실시간 공동편집

정적인 `onSnapshot` 덮어쓰기만으로 같은 글을 편집하지 말고 Yjs CRDT를 사용한다.

### 공유 문서 구성

- `Y.Text("body")`: 일기 원문
- `Y.Map("meta")`: 감정, 제목, 날짜, 주수/개월 수, 기록 종류, 작성자 정보
- `Y.Array("photoIds")`: 사진 순서
- `Y.Array("stickers")`: 스티커 인스턴스
- `Y.Map("aiApplied")`: 부모가 선택 적용한 AI 제안

### Firestore 연동

- `workspaces/{workspaceId}/records/{recordId}/collab/updates/{updateId}`에 압축된 Yjs update를 저장한다.
- 재접속 시 최신 snapshot을 먼저 읽고 이후 update를 순서와 무관하게 적용한다.
- 클라이언트 update는 250ms 내외로 batch하고 idempotent update ID를 사용한다.
- 일정 개수 이상의 update가 쌓이면 Cloud Function이 snapshot으로 압축한다.
- 필터에 필요한 `recordDate`, `recordType`, `emotion`, `weekOrAge`, `title`은 record 문서에 debounce하여 mirror한다.

### Presence

- 참여자 이름, 색상, 접속 여부, 커서 위치, 현재 선택 요소를 임시 presence 문서로 동기화한다.
- heartbeat를 보내고 일정 시간 업데이트가 없으면 오프라인으로 간주한다.
- presence는 영구 이력에 포함하지 않는다.

### 충돌과 이력

- 텍스트 동시 입력은 병합한다.
- 사진 순서와 스티커 속성도 Yjs 공유 타입으로 병합한다.
- 수동 저장, AI 적용, 삭제, 복원 같은 의미 있는 시점에는 history snapshot을 남긴다.
- 변경 이력 화면에는 참여자, 시각, 변경 유형을 표시한다.
- 복원 전 현재 상태와 복원 대상 시점을 보여주고 재확인한다.

## 18. 데이터 모델

Firestore 구조를 다음 의미로 설계해줘.

```text
workspaces/{workspaceId}
  ownerUid
  babyName
  dueDate
  birthDate
  stage
  createdAt

workspaces/{workspaceId}/members/{uid}
  role: owner | collaborator
  displayName
  color
  joinedAt

workspaces/{workspaceId}/records/{recordId}
  recordType
  recordDate
  weekOrAge
  emotion
  title
  authorIds[]
  photoCount
  milestoneType
  createdAt
  updatedAt
  deletedAt

workspaces/{workspaceId}/records/{recordId}/collab/snapshot
workspaces/{workspaceId}/records/{recordId}/collab/updates/{updateId}
workspaces/{workspaceId}/records/{recordId}/photos/{photoId}
workspaces/{workspaceId}/records/{recordId}/history/{historyId}
workspaces/{workspaceId}/presence/{uid}
workspaces/{workspaceId}/milestoneStories/{storyId}

invites/{tokenHash}
  workspaceId
  pinSalt
  pinHash
  expiresAt
  revokedAt
  failedAttempts
  lockedUntil
  createdBy
```

필터를 위해 다음 복합 인덱스를 준비한다.

- workspaceId + recordDate desc
- workspaceId + recordType + recordDate desc
- workspaceId + emotion + recordDate desc
- workspaceId + weekOrAge + recordDate desc
- workspaceId + milestoneType + status

## 19. 화면 목록

다음 화면과 하단 시트를 모두 구현한다.

1. 스플래시/소개
2. 최초 설정 1: 아기 이름과 임신/출산 단계
3. 최초 설정 2: 예정일 또는 생년월일
4. 최초 설정 3: 엄마·배우자 표시 이름
5. 가족 앨범 생성 완료
6. 초대 링크·6자리 PIN 생성
7. 초대 참여/PIN 입력
8. 타임라인 홈
9. 타임라인 필터 하단 시트
10. 기록 종류 선택
11. 일상 기록 작성
12. 검진 기록 작성
13. 태동 기록 작성
14. 편지 기록 작성
15. 사진 추가·순서 변경
16. 감정 선택과 필수 오류 상태
17. 추천 스티커/전체 스티커 하단 시트
18. AI 제안 미리보기·선택 적용
19. 실시간 공동편집 상태
20. 저장 완료
21. 감성 앨범 상세/사진 캐러셀
22. 특별 시기 성장 이야기 후보
23. AI 성장 이야기 초안 검토·수정
24. 완성된 성장 이야기 페이지
25. 공유 관리/링크 만료·재발급
26. 변경 이력과 복원
27. 빈 상태, 로딩, 오프라인, 업로드 실패, AI 실패, 초대 만료 상태

## 20. 오류와 빈 상태

- 타임라인 비어 있음: 첫 기록 CTA
- 필터 결과 없음: 필터 수정/초기화
- 사진 업로드 실패: 사진별 재시도/삭제
- AI 생성 실패: 재시도/AI 없이 계속
- 네트워크 끊김: 오프라인 배너, 로컬 대기, 복구 후 동기화
- 초대 PIN 오류: 입력란 아래 오류와 재시도
- 초대 링크 만료: 새 링크 요청 안내
- 같은 기록 삭제: 재확인 모달
- 복원: 현재 상태와 대상 시점 비교 후 확인

## 21. 데모 데이터

개발 모드에서 다음 예시 기록을 넣을 수 있는 seed 스크립트를 제공한다.

- 임신을 처음 확인한 날
- 첫 초음파를 본 날
- 첫 태동을 느낀 날
- 아기가 태어난 날
- 처음 웃어준 날

저작권 문제가 없는 단색/그라데이션 이미지 placeholder를 사용하고, 실제 사용자 사진처럼 보이는 외부 이미지를 임의로 가져오지 않는다.

## 22. 테스트와 완료 조건

다음 테스트를 작성하고 README에 실행 방법을 적어줘.

- 주수/개월 수 계산 단위 테스트
- 글/감정 필수값 검증 테스트
- 사진 5장 제한 테스트
- 필터 조합 테스트
- 초대 PIN 성공/실패/잠금 테스트
- Firestore/Storage 보안 규칙 테스트
- 두 Yjs 문서의 동시 텍스트/스티커 병합 테스트
- AI JSON schema 검증과 실패 fallback 테스트
- 소유자와 공동작업자 권한 테스트
- 모바일 390px 기준 핵심 화면 시각 회귀 또는 스냅샷 테스트

완성 후 다음을 제공해줘.

1. 실행 가능한 전체 코드
2. Firebase 설정과 배포 순서
3. 필요한 환경변수 목록
4. Emulator 실행 방법
5. seed 데이터 생성 방법
6. 두 브라우저에서 실시간 공동편집을 검증하는 시연 절차
7. 보안 규칙과 AI 키 보호 방식 설명

코드에 TODO만 남긴 가짜 구현, 로컬 상태만 바꾸는 공동편집 시뮬레이션, 클라이언트에 노출된 Gemini API 키는 허용하지 않는다. Firebase 설정값이 없는 미리보기 환경에서는 명확한 `데모 모드` 배너와 제한된 mock adapter를 제공하되, 실제 Firebase adapter와 동일한 인터페이스를 사용하고 배포 시 교체 없이 환경변수만으로 전환되게 만들어줘.
